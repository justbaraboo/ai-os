project: "AI Operating System"
version: "0.1.0"

principles:
  - chat_is_not_source_of_truth
  - skills_are_modular
  - decisions_are_logged
  - snapshots_are_mandatory

commands:
  CREATE_SNAPSHOT: "Creates structured snapshot"
  EXPORT_FULL_CONTEXT: "Exports system state"
